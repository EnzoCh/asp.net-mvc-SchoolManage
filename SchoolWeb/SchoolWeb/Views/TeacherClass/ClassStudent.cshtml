@model IEnumerable<SchoolWeb.Models.Score>
@{
    Layout = null;
}

<link rel="apple-touch-icon" sizes="76x76" href="~/Content/assets/img/apple-icon.png">
<link rel="icon" type="image/png" sizes="96x96" href="~/Content/assets/img/favicon.png">
<link href="~/Content/assets/css/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/assets/css/animate.min.css" rel="stylesheet" />
<link href="~/Content/assets/css/paper-dashboard.css" rel="stylesheet" />
<link href="~/Content/assets/css/themify-icons.css" rel="stylesheet">
<link href="~/Content/comment.css" rel="stylesheet">
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div id="mian">
                    <div id="title">
                        <h1>已选该课程学生名单</h1>
                    </div>
                    @using (Html.BeginForm())
                    {
                        <div id="search">
                            <div id="searchbut">@Html.ActionLink(" 返回课程", "Index", new { }, new { style = "font-size:25px;text-decoration:none;margin-top:25px;" })</div>
                        </div>
                    }
                </div>
                <div class="content table-responsive table-full-width cont">
                    <table class="table table-striped" id="sampleTableA">
                        <thead>
                            <tr>
                                <th>学号</th>
                                <th>姓名</th>
                                <th>分数</th>
                                <th>详情</th>
                                <th>操作</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (ViewBag.Data != null)
                            {
                                for (int i = 0; i < ViewBag.Data.Count; i++)
                                {
                                    <tr>
                                        <td>@ViewBag.Data[i].Sco_StudentNo</td>
                                        <td>@ViewBag.Data[i].Sco_Name</td>
                                        <td>
                                            @if (ViewBag.Data[i].Sco_Score == "")
                                            {
                                                <p>未打分</p>
                                            }
                                            else
                                            {
                                                <p>@ViewBag.Data[i].Sco_Score</p>
                                            }
                                        </td>
                                        <td>@ViewBag.Data[i].Exam_Name</td>
                                        <td>
                                            @if (ViewBag.Data[i].Sco_Score == "")
                                            {
                                                <p>@Html.ActionLink("打分", "update", new { Sco_StudentNo = ViewBag.Data[i].Sco_StudentNo })</p>
                                            }
                                            else
                                            {
                                                <p>已打分</p>
                                            }
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>

                </div>

            </div>
        </div>
    </div>
</div>

<script src="../../Content/js/jquery-1.11.0.min.js" type="text/javascript"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/js/bootstrap.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/js.geotools/dist/geotools.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/js.geotools/dist/geotools-swedish-zipcodes.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/moment@2.24.0/moment.min.js"></script>
<script src="../../Content/src/fancyTable.js"></script>
<script type="text/javascript">
    // Word genarator
    function rWord(r) { var t, n = "bcdfghjklmnpqrstvwxyz", a = "aeiou", e = function (r) { return Math.floor(Math.random() * r) }, o = ""; r = parseInt(r, 10), n = n.split(""), a = a.split(""); for (t = 0; t < r / 2; t++) { var l = n[e(n.length)], p = a[e(a.length)]; o += 0 === t ? l.toUpperCase() : l, o += 2 * t < r - 1 ? p : "" } return o }

    $(document).ready(function () {
        // And make them fancy
        $("#sampleTableA").fancyTable({
            sortColumn: 0,
            pagination: true,
            perPage: 5,
            globalSearch: true
        });
    });
</script>





@*<link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/modernizr-2.6.2.js"></script>

    <style>
        * {
            margin: 0px;
            padding: 0px;
        }

        body {
            background-color: #F5F5F5;
        }

        table {
            background-color: white;
        }

        .add button {
            margin-top: 20px;
            margin-left: 150px;
        }

        h1 {
            text-align: center;
        }

        .search {
            margin-top: 20px;
        }

        #divAdd {
            display: none;
        }

        #divAddInfo {
            border: 1px solid black;
            border-top: none;
            width: 500px;
            height: 300px;
            position: absolute;
            top: 200px;
            left: 500px;
            background-color: white;
        }

        #divTop {
            border-top: 1px solid black;
            border-left: 1px solid black;
            border-right: 1px solid black;
            width: 500px;
            height: 30px;
            background-color: #90E3F4;
            position: absolute;
            top: 170px;
            left: 500px;
        }

        .textadd {
            margin-top: 15px;
            margin-left: 15px;
        }

        .inputadd {
            margin-left: -20px;
            margin-top: 10px;
        }

        .btnadd {
            margin-left: 109px;
            margin-top: 20px;
        }

        .btnempty {
            margin-left: 50px;
            margin-top: 20px;
        }

        .btnclose {
            margin-left: 50px;
            margin-top: 20px;
        }
    </style>

    @using (Html.BeginForm())
    {
        <body>
            <div class="row main">
                <div class="col-md-6">
                    <h1>已选该课程学生名单</h1>
                </div>
                <div class="col-md-4 col-md-offset-2">
                    <span class="input-group-btn">
                        @Html.ActionLink(" 返回课程", "Index", new { }, new { style = "font-size:25px;text-decoration:none;margin-top:25px;", @class = "glyphicon glyphicon-circle-arrow-left " })
                    </span>
                </div>
            </div>
        </body>
    }
    <table class="table">
        <tr>
            <td>学号</td>
            <td>姓名</td>
            <td>分数</td>
            <td>详情</td>
            <td>操作</td>
        </tr>
        @if (ViewBag.Data != null)
        {
            for (int i = 0; i < ViewBag.Data.Count; i++)
            {
                <tr>
                    <td>@ViewBag.Data[i].Sco_StudentNo</td>
                    <td>@ViewBag.Data[i].Sco_Name</td>
                    <td>
                        @if (ViewBag.Data[i].Sco_Score == 0)
                        {
                            <p>未打分</p>
                        }
                        else
                        {
                            <p>@ViewBag.Data[i].Sco_Score</p>
                        }
                    </td>
                    <td>@ViewBag.Data[i].Exam_Name</td>
                    <td>
                        @if (ViewBag.Data[i].Sco_Score == 0)
                        {
                            <p>@Html.ActionLink(" ", "Change", new { Sco_StudentNo = ViewBag.Data[i].Sco_StudentNo })</p>
                        }
                        else
                        {
                            <p>已打分</p>
                        }
                    </td>
                </tr>
            }
        }
    </table>*@